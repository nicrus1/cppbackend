#include "player.h"

// Player — простая структура, все методы inline в .h
// Этот файл может быть пустым или содержать:

// Если у вас есть не-inline методы, раскомментируйте:
/*
Player::Player(int id, std::string name, const std::string& mapId, int dogId)
    : id_(id), name_(std::move(name)), mapId_(mapId), dogId_(dogId) {}

int Player::getId() const { return id_; }
const std::string& Player::getName() const { return name_; }
const std::string& Player::getMapId() const { return mapId_; }
int Player::getDogId() const { return dogId_; }
*/